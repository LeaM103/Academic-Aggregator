# Academic Resource Aggregator

Search OpenLibrary and arXiv for books and research papers.

## Run locally

### 1️⃣ Run two backend instances
```bash
cd backend
PORT=3000 node server.js   # Terminal 1
PORT=3001 node server.js   # Terminal 2
